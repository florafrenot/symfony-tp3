{% extends 'base.html.twig' %}

{% block title %}Color{% endblock %}

{% block body %}
{% include 'admin/nav.html.twig' %}

    <h1>Couleur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ color.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ color.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_color_index') }}">Revenir à la liste</a>

    <a href="{{ path('app_admin_color_edit', {'id': color.id}) }}">Éditer</a>

    {{ include('admin/color/_delete_form.html.twig') }}
{% endblock %}
