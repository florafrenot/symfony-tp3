<form method="post" action="{{ path('app_admin_type_delete', {'id': type.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir le supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
